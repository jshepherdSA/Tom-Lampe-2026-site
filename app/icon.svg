<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img">
  <!-- PLACEHOLDER FAVICON — TODO: replace with a real icon-only lockup (Q2).
       Lampe Indigo ground + wordmark initial. The "L" is drawn as geometry
       rather than set in Archivo because an SVG favicon cannot load a webfont. -->
  <title>Placeholder icon</title>
  <rect width="64" height="64" fill="#292F70"/>
  <path d="M18 14h10v27h18v9H18z" fill="#FFFFFF"/>
</svg>
